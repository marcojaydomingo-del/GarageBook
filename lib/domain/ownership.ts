const uuidPattern=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export function buildVehicleDocumentPath(ownerId:string,vehicleId:string,fileName:string,uniqueId=crypto.randomUUID()){if(!uuidPattern.test(ownerId)||!uuidPattern.test(vehicleId))throw new Error("Invalid ownership identifiers");const safeName=fileName.normalize("NFKD").replace(/[^a-zA-Z0-9._-]/g,"-").replace(/-+/g,"-").slice(0,180)||"document";return `${ownerId}/${vehicleId}/${uniqueId}-${safeName}`}
export function isProtectedPath(pathname:string){return pathname==="/dashboard"||pathname.startsWith("/dashboard/")||pathname==="/onboarding"||pathname.startsWith("/onboarding/")||pathname==="/reset-password"||pathname==="/shops"||pathname.startsWith("/shops/")||pathname==="/vehicles"||pathname.startsWith("/vehicles/")||pathname.startsWith("/documents/")}
