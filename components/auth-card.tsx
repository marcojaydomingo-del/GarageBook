import Link from "next/link";
import { Brand } from "./brand";
export function AuthCard({mode}:{mode:"login"|"signup"}){const signup=mode==="signup";return <main className="grid min-h-screen place-items-center bg-[#f3f4f0] px-4 py-10"><div className="w-full max-w-md"><div className="mb-8 flex justify-center"><Brand/></div><section className="card p-6 sm:p-8"><h1 className="text-center text-2xl font-semibold tracking-tight">{signup?"Create your GarageBook":"Welcome back"}</h1><p className="mt-2 text-center text-sm text-muted">{signup?"Start building a trusted history for your vehicles.":"Sign in to continue to your garage."}</p><form className="mt-7 space-y-4">{signup&&<div className="field"><label htmlFor="name">Full name</label><input id="name" autoComplete="name" placeholder="Jamie Rivera"/></div>}<div className="field"><label htmlFor="email">Email address</label><input id="email" type="email" autoComplete="email" placeholder="you@example.com"/></div><div className="field"><div className="flex items-center justify-between"><label htmlFor="password">Password</label>{!signup&&<button className="text-xs font-semibold text-teal" type="button">Forgot password?</button>}</div><input id="password" type="password" autoComplete={signup?"new-password":"current-password"} placeholder="••••••••"/></div><Link className="btn btn-primary mt-2 w-full" href="/dashboard">{signup?"Create account":"Log in"}</Link></form><p className="mt-6 text-center text-sm text-muted">{signup?"Already have an account? ":"New to GarageBook? "}<Link className="font-semibold text-teal" href={signup?"/login":"/signup"}>{signup?"Log in":"Create account"}</Link></p></section><p className="mt-5 text-center text-xs text-muted">Prototype authentication screen · Supabase connection ready</p></div></main>}
