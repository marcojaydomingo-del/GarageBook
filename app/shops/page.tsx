import { MapPin, Phone, Plus, Star, Wrench } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { PageHeader } from "@/components/page-header";
import { formatCurrency, shops } from "@/lib/sample-data";
export default function ShopsPage(){return <AppShell><PageHeader eyebrow="Service network" title="Repair shops" description="Keep a dependable record of every shop that has worked on your vehicles." actions={<button className="btn btn-primary"><Plus size={16}/>Add shop</button>}/><div className="grid gap-5 lg:grid-cols-2">{shops.map(shop=><article className="card p-5 sm:p-6" key={shop.id}><div className="flex justify-between gap-4"><span className="grid h-12 w-12 place-items-center rounded-xl bg-[#e7f3f0] text-teal"><Wrench size={20}/></span>{shop.preferred&&<span className="status status-good h-fit"><Star size={12} className="mr-1"/>Preferred</span>}</div><h2 className="mt-5 text-lg font-semibold">{shop.name}</h2><p className="mt-1 text-sm text-muted">{shop.specialty}</p><div className="mt-5 space-y-2 text-sm text-muted"><p className="flex gap-2"><MapPin size={16} className="mt-0.5 shrink-0"/>{shop.address}</p><p className="flex gap-2"><Phone size={16}/>{shop.phone}</p></div><div className="mt-6 grid grid-cols-3 border-t border-[#edf0ed] pt-5"><ShopStat label="Visits" value={String(shop.visits)}/><ShopStat label="Last visit" value={shop.lastVisit}/><ShopStat label="Total spend" value={formatCurrency(shop.totalSpend)}/></div><button className="btn btn-secondary mt-5 w-full">View shop history</button></article>)}</div></AppShell>}
function ShopStat({label,value}:{label:string;value:string}){return <div><p className="text-[11px] text-muted">{label}</p><p className="mt-1 text-xs font-semibold sm:text-sm">{value}</p></div>}
